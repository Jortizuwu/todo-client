import 'react-native-gesture-handler'
import React from 'react'
import { Icon, IIconProps } from 'native-base'

function Logo(props: IIconProps) {
  return <Icon viewBox="0 0 602.339 681.729" {...props} />
}

export default Logo
